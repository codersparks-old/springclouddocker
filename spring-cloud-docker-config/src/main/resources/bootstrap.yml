spring:
  application:
    name: SCDConfig
  cloud:
    config:
      discovery:
        enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://${SCD_EUREKA_HOST}:${SCD_EUREKA_PORT}/eureka/
