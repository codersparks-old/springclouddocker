spring:
  application:
    name: SCDConfig
  cloud:
    config:
      discovery:
        serviceId: SCDConfig
        enabled: false
    failFast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://${SCD_EUREKA_HOST}:${SCD_EUREKA_PORT}/eureka/
