spring:
  application:
    name: SCDUUID
  profiles:
    active: 'DEV'
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: SCDConfig
      failFast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_SERVER_HOST}:${EUREKA_SERVER_PORT}/eureka/